//Report for Part 3 of Project 2

To load the module, run "make all", and then run "insmod print_other.ko" to load the module. 
The command dmesg will show it displaying the process names, PID's, and state up to init. 
After running, "rmmod print_self" will unload the module. 
A dmesg command should show a message confirming unloading.
